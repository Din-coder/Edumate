extends layout
block content
  .container
    h1 Error
    p We're sorry, but an error has occurred.

    if message
      p Error message: #{message}

    a.btn.btn-primary(href="/") Go back to home page